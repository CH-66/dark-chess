# 浏览器完整交互验收记录

## 验收定位

V0.3 在规则引擎 32/32 自动化测试通过后，进一步验证浏览器端的可玩闭环。验收对象是实际 DOM、按钮、棋盘点击、动画结束后的状态和页面日志。

## 自动化验收项

| 编号 | 验收项 | 验收动作 | 结果 |
|---|---|---|---|
| B-01 | 开局 | 打开页面并检查 32 枚棋子、2 明棋王、30 暗棋、红方先手 | PASS |
| B-02 | Seed | 输入固定 seed 并重新开局，检查页面展示与输入值保持一致 | PASS |
| B-03 | 选择暗棋 | 点击己方暗棋，检查选中态、移动提示、翻棋按钮 | PASS |
| B-04 | 原地翻棋 | 点击“原地翻开”，检查揭示、回合切换、手数、日志 | PASS |
| B-05 | 暗棋移动 | 点击暗棋与合法目标，检查移动、自动揭示、回合切换、日志 | PASS |
| B-06 | 吃暗棋 | 点击车和暗棋目标，检查被吃棋子消失且不泄露真实身份 | PASS |
| B-07 | 吃帅/将 | 点击合法吃子目标，检查胜方、结束状态、提示与日志 | PASS |
| B-08 | 重新开局 | 点击重新开局，检查手数归零、日志重置、红方先手 | PASS |

## 测试隔离

为了稳定复现随机对局难以直接构造的边界局面，页面仅在 URL 参数 `?e2e=1` 下暴露测试状态注入接口。正式访问不启用该接口；真正的验收步骤仍通过浏览器 DOM 完成点击和断言。

## CI

GitHub Actions 在 Ubuntu 24.04 + Node.js 24 环境中执行 `npm install`、安装 Chromium、运行规则测试和 `npm run test:e2e`。失败时上传 `test-results/` 诊断产物。

## 记录原则

本文件与 Notion 均采用追加记录，不覆盖之前的规则测试和失败记录。
## 2026-09-18｜第一次浏览器实测

- Actions 首轮浏览器 E2E 未通过；失败来自验收脚本，而非规则引擎。
- 失败 1：点击黑方棋子时并非黑方回合；修正为红方先手场景。
- 失败 2：点击吃子高亮层时，实际棋子覆盖该层并拦截 pointer event；修正为模拟真实用户直接点击被吃棋子。
- 失败 3：重新渲染后旧 locator 仍指向旧的选择器条件；改为基于稳定 `data-piece-id` 重新定位。

## 2026-09-18｜最终浏览器验收结果

- GitHub Actions Run #34：**4/4 Playwright E2E PASS**。
- 同一次运行中的 `npm test`：**32/32 PASS**。
- 浏览器环境：Chromium / Ubuntu 24.04 / Node.js 24.20.0。
- 已验证浏览器实际交互链路：开局 → 固定 seed → 选择暗棋 → 原地翻棋 → 暗棋移动后自动翻开 → 吃暗棋 → 吃帅/将 → 重新开局。
- PR #6 已合并到 `main`，合并提交：`a2ee7889ca2338771275f3d4aa2f9e6384c9c3c4`。

## 后续验收

- 浏览器自动化已成为 CI 固定门槛；后续 UI/规则变更应继续通过 `npm test` + `npm run test:e2e`。
- 下一阶段重点转向浏览器人工体验验收与联网对战前的架构设计。
## 2026-09-19｜扩展验收结果

- 浏览器 E2E 已扩展为 **8/8 PASS**：增加 Seed 实际复现、暗棋 DOM 不泄密、非己方棋子不可选、终局后禁止操作、移动端无横向溢出。
- 同次 GitHub Actions 的规则/不变量测试为 **46/46 PASS**。
