{
  "name": "dark-chess",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:coverage": "node --experimental-test-coverage --test-coverage-lines=100 --test-coverage-functions=100 --test-coverage-branches=98 --test tests/*.test.js",
    "test:e2e": "playwright test",
    "start": "node server/index.js",
    "dev": "node server/index.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=24.20.0 <25"
  }
}
